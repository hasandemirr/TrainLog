# S2 — Kayıt döngüsü — Sprint Notu

**Tarih:** 2026-08-14 · **Akış:** `main` üstünde küçük commit'ler; push sahibe (D50).
Bu not mimar incelemesinin girdisidir.

## 1. Kapsam & sonuç (ROADMAP S2, F1.1-F1.6 + tohum)

| Kalem | Durum | Dosya |
|---|---|---|
| Sürümlü tohum (22 hareket/5 gün) | ✅ +8 test | `content/seed.ts` |
| Seans doğumu (D17) + döngü önerisi (D44) | ✅ +7 test | `domain/session.ts` |
| Store (tek-yönlü akış) + aksiyonlar + ekim | ✅ +11 test | `app/store.ts`, `app/actions.ts` |
| Türetilmiş indeksler (geçen seans/hedef/ipucu/düşüş) | ✅ +6 test | `app/selectors.ts` |
| Kayıt döngüsü UI (önerilen gün, hareket kartı, set girişi, RIR/not) | ✅ | `ui/views/WorkoutView.tsx`, `ui/components/NumInput.tsx`, `ui/format.ts` |

**Kapı md.1:** `typecheck` temiz; `vitest` **69 test** yeşil; domain testleri S2 şartnamesini kapsıyor. `build` yeşil, bundle **JS ~12.5 KB gzip** ≪ 150 KB.

## 2. Mimar odakları — durum

- **① Tohum tek kaynak** — **Kanıtlanan:** `content/seed.ts` `SEED` (SeedCatalog) **ekim (`sow`)** tarafından kullanılır. **Hedeflenen (henüz kanıtlanmadı):** `migrateV1` enjeksiyonu aynı `SEED`'i alacak, ama S2'de **hiçbir uygulama yolu `migrateV1` çağırmıyor** (yalnızca test, sentetik tohumla) — **çağrı yeri (içe alma/geri yükleme) S3'tedir**. Tek-kaynak kanıtı S3'te içe alma yolu `SEED` import'uyla bağlanınca üretilecek. 22/5 sayıları testle sabit.
- **② Seans doğumu D17** — `createSession` tarihi **param** alır (açılışta sabit), "şimdi"den türetmez; test: sonraki aksiyon (çok ileri updatedAt) seansın tarihini değiştirmiyor. Seans **ilk kayıtla** doğar (reduce upsert).
- **③ D24 senkron persist** — `store.dispatch` her aksiyonda `storage.save` (senkron) → çiz. Testle + **önizlemede** kanıtlandı: set gir → reload → veri **ve gösterim** duruyor (input `62,5`/`8`).
- **④ D8 türetme** — `selectors` (workoutModel, lastRecordForExercise, exerciseCard) **saf**, **asla kalıcılaşmaz**; seçili gün/hafta **UI param** (bileşen state'i), AppState'e girmez.

## 3. Mimarın bakması gereken karar noktaları

- **Ekim koşu açar:** `sow`, tohum programı ailesi için koşu yoksa bir koşu açar (`startDate = bugün, yerel`). Kayıt döngüsünün çalışması için koşu gerekiyordu; program yönetimi/koşu başlatma S5. Kabul mü?
- **Reload'da güne devam:** `workoutModel` varsayılan günü, bugün **bitmemiş** (finishedAt yok) bir seans varsa ona ayarlar (reload'da in-progress gün kaybolmasın). D44 "son **tamamlanan**" gereği: tamamlama + oto-kapanış (D46, "ileri tarihte açılınca") S3'te gelince ileri tarihte otomatik ilerleme oturur. S2'de finishedAt hep boş.
- **setSet alan-patch:** aksiyon tek alan (`{kg}` veya `{reps}`) taşır; reduce CANLI kayda uygular → aynı tuşta iki commit birbirini ezmez. (Önizlemede kg'nin reps tarafından ezildiği bulundu, düzeltildi + regresyon testi.)
- **Progression "beklet" gerekçeleri** domain'de kod; UI'da Türkçe eşleme (`HOLD_TEXT`). Artır ipucu: `+2,5 kg` / `+10 sn`.
- **Program kimlik/ad:** `prog_main_v1` / `fam_main`, ad "Program". Tohum içeriği (adlar, sayılar, 52sn dinlenmeler) mimarın normatif bloğu, bayt-bayt.
- **Kalıcılaştırma sıklığı:** her tuşta (D24). İskala küçük; sorun değil. NumInput tamponlu (yazarken kavga etmez, dıştan "geçeni al" değişimini yansıtır).

## 4. Bilinmeli

- **SW'ye dokunulmadı** — `src/pwa/` S1'den beri aynı. In-app tarayıcı bu oturumda da SW kaydı yapamıyor (harness); uygulama SW'siz sorunsuz çalışır (kayıt döngüsü + localStorage). D24 doğrulaması SW gerektirmez, in-app tarayıcıda yapıldı. Konsolda yalnızca harness SW hatası var, uygulama hatası yok (D35).
- **UI testi yok** (D34); kayıt döngüsü domain/app testleriyle + önizleme ile doğrulandı.

## 5. KL-C 4 — sahibin cihazda denemesi

Gerçek bir antrenman uçtan uca: önerilen gün → set/tekrar gir (virgüllü ondalık) → set ekle → RIR/not → "geçeni al" → kapat/aç, veri duruyor mu. Gün seçiciyle Home 1 (time türü: yalnız saniye) ve diğer günler.

## 6. Sonraki (S3 — tamamlama + ilk açılış, F1.7-F1.11/F0)

Dinlenme sayacı (D42-43), Wake Lock, ikame + yerinde yeni hareket (D47), serbest yuva + `#2` telafi, "Bitir" + özet + oto-kapanış (D46 — bu, reload/ileri-tarih ilerlemesini de tamamlar), ilk açılış + yedek dışa/içe (D27).
