# S3 — Tamamlama + ilk açılış + yedek — Sprint Notu

**Tarih:** 2026-08-14 · **Akış:** `main` üstünde küçük commit'ler; push sahibe (D50).
Bu not mimar incelemesinin girdisidir.

## 1. Kapsam & sonuç (ROADMAP S3, F1.7-F1.11 + F0 + yedek)

| Kalem | Durum | Yer |
|---|---|---|
| Sıfırıncı iş: tohum tek-kaynak kanıtı | ✅ | `app/backup.ts` (migrateV1(SEED)) + test + önizleme app-path |
| Tamamlama + oto-kapanış + özet (D46) | ✅ +5 test | `actions.ts`, `session.ts` (closeStale), `selectors.ts` (sessionSummary) |
| Yedek zinciri: içe (merge D27) + dışa (share→indirme→metin) + F0.2 | ✅ +5 test | `app/backup.ts`, `adapters/backup.file.ts`, `ui/components/{BackupImport,FirstRunBanner}` |
| Dinlenme sayacı (D42/43) + Wake Lock | ✅ +4 test | `actions.ts` (timer), `ui/components/{TimerBar,WakeLockToggle}` |
| İkame (D47) + yerinde yeni hareket | ✅ +6 test | `actions.ts`, `ids.ts` (makeExerciseId), `ui/components/SubstituteSheet` |
| Telafi `#2` (gün-çözücüsü içinden) | ✅ | `session.ts` (nextSeq) + `selectors.resolveSession` — ayrı yol yok |

**Kapı md.1:** `typecheck` temiz; `vitest` **89 test** yeşil; domain/app testleri S3 şartnamesini kapsıyor. `build` yeşil, bundle **JS ~16,5 KB gzip** ≪ 150 KB.

## 2. `tEnd` — D8 istisnası (gerekçesiyle)

`timer?: {tEnd,label}` `AppState`'te durur; bu **bilinçli bir D8 istisnasıdır**: türetilmiş
değil, yeniden kurulamayan geçici gerçektir — bellekten atılma/kilit sonrası sayaç ancak
**depodan** doğru dönebilir (D42'nin bütün amacı bu; mutlak `tEnd`). **Sayaç durumu yedeğe
girmez** — dışa aktarmada `toBackupState` ile soyulur (yarım sayacın yedek dosyasında işi
yok). **Merge'de yerel kazanır** (`merge` base.timer'ı korur).

## 3. Dört uygulama şartı — kanıtlar

1. **Paylaşım jesti (KL-C 5):** `adapters/backup.file.ts` — `onClick → exportNow → exportBackup` yolunda **hiçbir `await` yok**; `File` + `canShare` senkron, `share()` ilk asenkron sınır; fallback zinciri (indirme → metin) tek fonksiyonda, jest-senkron gövde yorumla işaretli. *Yapı kısıtı (test değil); Playwright yakalayamaz.*
2. **`tEnd` D8 istisnası:** §2'deki üç cümle. Testli: sayaçlı durum JSON round-trip'te `tEnd` korunur; `toBackupState` soyar; `merge` yereli tutar.
3. **İkame slug çakışması:** `makeExerciseId` — ad→slug (Türkçe translit); mevcut kimlikle çakışırsa `_2`, `_3` soneki. Test: ikinci "Barbell Row" → `ex_barbell_row_2`. Yeni hareket `userModified`. Liste **önce aynı bölge**, arşivsiz.
4. **F0.2 × oto-kapanış:** `importBackup` merge'den **sonra `closeStale`** koşar. Test: 2020 tarihli yedek → seans kapalı, önerilen gün Gün 2 (devam değil).

## 4. Önizleme doğrulamaları (gerçek app-path)

- **İçe alma app-path (tek-kaynak bağı):** FirstRunBanner → yükle → `importBackup(SEED)` → merge → `store.replace` → kayıt `{62,5×8}` geri geldi.
- **Sayaç:** Dinlen → çubuk "Dinlenme: 150 sn · Barbell Row", `tEnd` depoda.
- **İkame:** liste açılıyor; **Bitir → "Seans özeti · Pull · 1 set · hacim 480"**, `finishedAt` yazıldı.
- Konsolda yalnızca harness SW hatası (uygulama SW'siz çalışır); uygulama hatası yok.

## 5. Sahibe devir — cihaz kalemleri (KL-C 5-7, 9)

Gerçek Chromium/Playwright değil, **cihazda** doğrulanacak (S1 kuralı: SW/paylaşım gerçek ortamda):
- **KL-C 5:** Dışa aktar → **paylaşım sayfası → Dosyalara Kaydet** (jest-senkron zincir; masaüstünde görünmeyen iOS jest-penceresi burada sınanır).
- **KL-C 6:** Yedekten **birleştirmeli** geri yükleme (in-app önizlemede app-path çalıştı; cihazda dosya yoluyla).
- **KL-C 7:** Sayaç: kilitle → aç → kalan süre doğru; bitişte görsel uyarı (ses yok). Wake Lock anahtarı etkisi.
- **KL-C 9:** Safari sekmesi ↔ kurulu uygulama bölme ayrımı ilk-açılış (FirstRunBanner → yükle) yoluyla aşılıyor mu.

## 6. Karar/dikkat noktaları

- **Ekim koşusu** hâlâ `sow`'da açılıyor — **S5 devir maddesi:** ilk koşu S5'te birinci sınıf olacak, özel-durum yasak (bellekte not).
- **Özet** türetilmiş görünüm (D8), saklanmaz; takvimden geçmiş seanslar için de aynı özet (S4).
- **markBackup** dışa aktarımda `meta.lastBackup` yazar; 21 gün hatırlatması **S6** (D29).
- Sayaç ve güncelleme çubukları ikisi de alt barın üstünde — nadiren aynı anda; çakışırsa üst üste biner (S6 cilası).
