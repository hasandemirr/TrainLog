# S4 — İlerleme — Sprint Notu

**Tarih:** 2026-08-27 · **Akış:** `main` üstünde küçük commit'ler; push sahibe (D50).
Bu not mimar incelemesinin girdisidir.

## 1. Kapsam & sonuç (ROADMAP S4, F2)

| Kalem | Durum | Yer |
|---|---|---|
| Hareket bazlı kg/hacim eğrisi + seans listesi (F2.1) | ✅ | `selectors.exerciseSeries`, `ui/components/LineChart`, `ui/views/ProgressView` |
| Takvim ay görünümü + gün özeti (F2.2) | ✅ | `selectors.sessionsByDate` + `sessionSummary` reuse, `ui/components/SummaryCard` |
| Genel istatistik (F2.3) | ✅ | `selectors.generalStats` |
| Ölçüm segmenti (F2.4) | ✅ +5 test | `actions.setMeasure`, `selectors.measure*`, ProgressView |
| İçe alma sonuç özeti (focus 6) | ✅ +1 test | `app/backup.ts` MergeStats, `BackupImport` |

**Kapı md.1:** `typecheck` temiz; `vitest` **95 test** yeşil; selector'lar (eğri/istatistik/ölçüm) fixture'a karşı testli — kapı odağı "grafikler fixture'a karşı doğrulanır" karşılandı. `build` yeşil, bundle **JS ~19 KB gzip** ≪ 150 KB.

## 2. Mimar odakları — durum

1. **Grafikler bağımlısız** — `LineChart` elle SVG (chart kütüphanesi yok, bağımlılık sınırı). **Erişilebilirlik:** eğri `role="img"`+`aria-label`; altında **seans listesi metin** olarak durur (`serieslist`).
2. **Peşin D44 kararı uygulandı** — takvim ay görünümü sütun başlıkları hafta günü kısaltması taşır (Pt/Sa/Ça/Pe/Cu/Ct/Pz) — takvim gerçekliği. **Program gün çipleri hâlâ hiçbir yerde hafta günü göstermiyor** (WorkoutView `label` = Pull/Home 1/…). Proposal turu açılmadı.
3. **Takvim → özet** — güne dokununca `sessionSummary` açılır; **ikinci çağrı yeri**, `SummaryCard` bileşeni "Bitir" ile ortak — kopya özet mantığı yok (D8/D46).
4. **Ölçüm (F2.4)** — İlerleme sekmesi içinde; giriş (virgüllü ondalık D22, `NumInput`) + geçmiş + eğilim. `setMeasure`: measures tarih anahtarlı, aynı tarihe alan yazımı **son-yazan-kazanır** (v1 göç politikasıyla tutarlı); null → alanı, boş satır → tarihi temizler.
5. **Boş durumlar** — her segment veri yokken anlamlı Türkçe metin ("Henüz kayıt yok…", "Bu günde seans yok.", "Henüz ölçüm yok…"); grafik sıfır-veriyle çizilmez (`values.length > 0` kapısı).
6. **İçe alma özeti** — `importBackup` MergeStats döndürür; UI tek satır ("İçe alındı: N yeni kayıt, M güncelleme" / "Yedek mevcut veriyle aynıydı"). İnteraktif çakışma yok — sessiz LWW (D11'e dek).

## 3. Önizleme doğrulamaları

- **Hareket:** "1 seans · 1 kayıt · hacim 960 · 1 hareket"; seçici Barbell Row; SVG eğri + liste "2026-08-27 · 60×8, 60×8 · hacim 960".
- **Takvim:** ay 2026-08, başlıklar Pt…Pz, gün 27 işaretli → dokun → "Pull · 2026-08-27 · 2 set · hacim 960".
- **Ölçüm:** Kilo 78,5 → `{2026-08-27:{kilo:78.5}}` + "Kilo — eğilim".
- Konsolda yalnızca harness SW hatası; uygulama hatası yok.

## 4. Bilinmeli

- **`measures` merge** (yedek geri yükleme): S1 `merge` `byStable` ile birleştirir (measures'ta updatedAt yok); focus 4'ün LWW'si **giriş** yolundadır (setMeasure). Yedek-merge'de tarih çakışması belirlenimci seçimle çözülür. (Not: değişiklik değil, mevcut davranış.)
- **UI testi yok** (D34); ilerleme selector'larla + önizleme ile doğrulandı.
- Takvim ayı `today`'in ayından başlar; ‹ › ile gezinir (yalnız görüntü, durum değil).

## 5. Sonraki (S5 — program yönetimi, F3)

Aktif program görünümü, düzenleme → yeni sürüm + işaretçi (D15), yeni program + **koşu başlatma**, hareket kataloğu. **Devir maddesi (bellekte):** ekimin açtığı ilk koşu S5'te birinci sınıf vatandaş olacak — koşu başlatma genel yolla, özel-durum yasak; `sow`'daki koşu-açma bu genel aksiyona taşınmayı değerlendir.
