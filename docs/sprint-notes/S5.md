# S5 — Program yönetimi — Sprint Notu

**Tarih:** 2026-08-27 · **Akış:** `main` üstünde küçük commit'ler; push sahibe (D50).
Bu not mimar incelemesinin girdisidir.

## 1. Kapsam & sonuç (ROADMAP S5, F3 + devir)

| Kalem | Durum | Yer |
|---|---|---|
| Program sürüm zinciri (görüntüle/düzenle) | ✅ +7 test | `domain/program.ts`, `actions.saveProgramVersion`, `ui/views/ProgramView` |
| Yeni program + koşu; tek-aktif-koşu | ✅ | `actions.startRun`, `domain/program.openRun`, `Run.endedAt` |
| Katalog (oluştur/düzenle/arşivle, silme yok) | ✅ | `actions.addExercise/archiveExercise`, ProgramView Katalog |
| Öngörü doğrulaması | ✅ | `domain/program.validatePrescribed/validateProgram` |
| Devir: ekim koşusu birinci sınıf | ✅ | `sow` → `openRun` (startRun ile aynı yol) |

**Kapı md.1 + S5 kapı odağı:** `typecheck` temiz; `vitest` **102 test** yeşil; **koşu ortası düzenleme geçmiş seansların hedefini değiştirmiyor** domain testiyle (sessionSummary kanıtı) gösterildi. `build` yeşil, bundle **JS ~20 KB gzip** ≪ 150 KB.

## 2. Mimar odakları — durum

1. **Sürüm zinciri (D15):** düzenleme = yeni `Program` (yeni id `<familyId>_r<rev+1>`, aynı familyId, rev+1, `userModified`) + koşunun `currentProgId` işaretçisi ilerler; geçmiş seanslar eski `progId`'de kalır. Kapı testi + önizleme (sürüm 1→2, iki program kayıtta).
2. **Tek aktif koşu:** `Run.endedAt?` (salt-eklemeli). Aktif = `endedAt` yok, en fazla bir. `openRun` mevcut aktifi kapatır + yeniyi ekler; yeni koşuda öneri **Gün 1 / hafta 1**'den başlar (testli). **Şema kuralı (sprint notu):** salt-eklemeli isteğe bağlı alan şema sürümünü artırmaz — **`v:2` kalır**, `validate` güncellendi, eski veri geçerliliğini korur; kırıcı değişiklik `vN→vN+1` zinciriyle (D20).
3. **Katalog:** silme yok (referans bütünlüğü); `archiveExercise` → `archived:true` + `userModified` (re-sow dirltmez); arşivli `substitutionOptions`'tan ve yeni sürüm eklemeden dışlanır, geçmişte yaşar. Düzenleme `userModified` işaretler.
4. **Öngörü doğrulaması:** `lo≤hi`, `sets≥1`, `rest>0` — domain **kod** döner; UI Türkçe ("alt > üst" vb.) ve geçersizken **Kaydet kilitli** (önizlemede doğrulandı).
5. **UI yerleşimi (D45):** Program sekmesi **salt-okunur** açılır; Düzenle/Katalog/Yeni koşu/Yeni program bilinçli adımların arkasında — günlük yüzeye sızmaz.

## 3. Devir maddesi — ekim koşusu birinci sınıf

`sow` artık koşuyu `openRun` genel yoluyla açıyor (startRun ile **aynı** kod). Ekim koşusu:
**bitirilebilir** (startRun mevcut aktifi kapatır — testli), **programı düzenlenebilir**
(saveProgramVersion herhangi bir aktif koşuda çalışır), **özel-durum kodu yok**. Devir maddesi kapandı.

## 4. Önizleme doğrulamaları

- Program salt-okunur "sürüm 1" + [Düzenle/Katalog/Yeni koşu/Yeni program].
- Düzenle → "üst=5" (alt 6) → **"alt > üst"**, Kaydet kilitli → geçerli → **"sürüm 2"**, katalogda 2 program.
- Katalog → "Barbell Row" arşivle → arşiv işareti, **toplam 22 (silinmedi)**.

## 5. Bilinmeli / sonraki (S6 — ayarlar + yayın cilası, F4)

- Düzenle formu şu an sayısal alanları (set/alt/üst/dinlenme) düzenliyor; RIR düzenleme + güne hareket ekle/çıkar S6 cilası kapsamına alınabilir (mimar kararı).
- **S6:** yedek yaşı + 21 gün hatırlatması (D29), CSV dışa aktarma (D28), kişisel bilgiler, kullanım kılavuzu, "tüm verileri sil" (çift onay), erişilebilirlik/bütçe son geçişi → **1.0 mührü**.
