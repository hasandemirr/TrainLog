# S6 — Ayarlar + yayın cilası — Sprint Notu

**Tarih:** 2026-08-27 · **Akış:** `main` üstünde küçük commit'ler; push sahibe (D50).
Bu not mimar incelemesinin girdisidir. **Son sprint** — kapı geçilince 1.0 mührü.
**Güncelleme (aynı gün, mimar eki):** S6 kapanışından sonra mimar dört iş daha bağladı;
§8 bu ek turu anlatır ve aşağıdaki sayılar (test, bütçe, KL-C) ek dahil güncellenmiştir.

## 0. Süreç kuralı (yeni, mimar tarafından eklendi)

**Sprint sınırı = oturum sınırı.** Her sprint taze bir Claude Code oturumunda başlar;
oturum, CLAUDE.md okuma sırasını (DECISIONS → ARCHITECTURE → ROADMAP → sprint notları)
baştan uygular. Sprint notu bu yüzden yalnızca özet değil, **devir belgesidir**: bir
sonraki oturumun bağlamı budur. S6 bu kuralla açıldı.

## 1. Kapsam & sonuç (ROADMAP S6, F4 → 1.0)

| Kalem | Durum | Yer |
|---|---|---|
| Yedek yaşı + 21 gün hatırlatması (D29) | ✅ +5 test | `app/backup.ts` `backupStatus`, `ui/components/BackupReminder`, Ayarlar |
| CSV dışa aktarma (D28) | ✅ +10 test | `app/csv.ts` `buildCsv`, `adapters/backup.file.ts` `exportFile/exportCsv` |
| Kullanım kılavuzu (F4.4) | ✅ | `ui/components/Guide.tsx` (statik içerik) |
| Kişisel bilgiler, asgari (F4.5) | ✅ +8 test | `AppState.profile?`, `setProfile`, Ayarlar bölümü |
| Tüm verileri sil, çift onay (F4.6) | ✅ +4 test | `app/store.ts` `freshState`, `main.ts` `wipeAll`, Ayarlar tehlikeli bölge |
| Erişilebilirlik geçişi | ✅ (ölçümle) | `ui/styles.css`, `BackupImport`, `ProgressView`, `WakeLockToggle` satırı |
| Bütçe son kontrolü (D35) | ✅ (ölçümle) | §5 |
| **Ek:** F3.2'nin eksik kalanı (RIR + yuva ekle/çıkar/sırala) | ✅ +9 test | `domain/program.ts`, `ui/views/ProgramView` — §8 |

**Kapı md.1:** `typecheck` temiz; `vitest` **138 test** yeşil (S5: 102 → +36).
**Kapı md.2:** `build` yeşil; Playwright dumanı 2/2; bütçeler §5 — fazlasıyla altında.

## 2. Mimarın beş odağı — durum

### 1. CSV üretimi domain/app'te saf fonksiyon, testli

`app/csv.ts` → `buildCsv(state): string`. DOM/dosya/tarayıcı API'si yok; dosyaya
indirme adapters'ta. Sütun düzeni normatif sabit `CSV_HEADER`:

```
Hafta;Tarih;Gün;#;Hareket;Hedef Set;Hedef Tekrar;Hedef RIR;
S1 kg;S1 tkr;…;S5 kg;S5 tkr;Toplam Hacim;Gerçek RIR;Not
```

- Ayraç `;`, dosya başında BOM, satır sonu CRLF (Excel tr).
- Ondalık **virgül** (D22) — ayraç `;` olduğu için güvenli. Alan kaçışı: `;`, `"`,
  CR/LF içeren değer tırnaklanır, tırnak ikilenir.
- **5'ten çok set:** ilk 5'i yazılır, **Not** sütununa `+N set CSV'ye sığmadı` eklenir
  (kullanıcının notu korunur, ` · ` ile ayrılır). **Toplam Hacim TÜM setlerden** hesaplanır
  — kırpma sayıyı bozmaz (testli).
- **Hedef sütunları seansın SOMUT program sürümünden** gelir (D15) — koşu ortası
  düzenleme geçmiş satırların hedefini değiştirmiyor (testli, S5 kapı testinin CSV'deki karşılığı).
- **İkame edilmiş slotta hedef sütunları boş** (D47): program hedefi o harekete ait değil.
  *Karar noktası:* alternatif, slotun hedefini yine de yazmaktı; yanıltıcı bulduğum için
  boş bıraktım. Mimar aksini isterse tek satırlık değişiklik.
- Boş kayıt satır üretmez; satırlar seans (tarih#sıra) sonra slot sırasında.
- **CSV yedek SAYILMAZ:** `exportCsvNow` `markBackup` dispatch etmez → yedek yaşını
  sıfırlamaz (önizlemede doğrulandı). D28'in "yalnız dışa" kuralı: karşılık gelen bir
  parse fonksiyonu yok, kurtarma biçimi JSON'dur; Ayarlar'daki metin bunu söylüyor.

### 2. "Tüm verileri sil" store.replace + taze-init, SW önbelleğine dokunmuyor

`freshState(seed, deps) = sow(emptyState(deps))` — **ilk açılışla aynı yol**; test
`freshState(...)`'in `initState(...)` çıktısına eşit olduğunu gösteriyor (özel-durum kodu yok,
S5'teki `sow → openRun` devir maddesinin devamı). `main.ts` `wipeAll`: `storage.clear()`
→ `store.replace(freshState(...))` (senkron persist, D24). **Hiçbir `caches` çağrısı yok**
(D36). Gerçek Chromium'da ölçüldü: silme öncesi/sonrası `caches.keys()` = `["trainlog-…"]`
**aynı**. Çift onay: düğme → uyarı paneli → ikinci düğme; ilk adım veriye dokunmuyor (ölçüldü:
onay panelindeyken kayıt sayısı hâlâ 1).

### 3. 21-gün banner'ı yalnız kayıt varken

`backupStatus(state, now)` → `{lastBackup, ageDays, hasData, remind, reason}`.
`remind = hasData && (never | ageDays ≥ 21)`. Kayıt yoksa `remind:false` (boş uygulamada
rahatsız etmez) — gerekçe yine hesaplanır ama uyarı çıkmaz. Eşik **dahil** (20 hayır, 21 evet),
yaş gün tabanına yuvarlanır, ileri tarihli yedek negatif yaş üretmez.
**Semantik korundu:** metin "N gündür yedek yok" / "Hiç yedek almadın" — tahliye tahmini
değil. Banner yalnızca Antrenman ekranının başında; "Şimdi değil" **oturumluk UI durumu**
(D8, AppState'e girmez). Ayarlar'da yaş her zaman görünür (D29).

### 4. Kılavuz statik içerik — AppState'e girmiyor

`ui/components/Guide.tsx` içinde modül düzeyinde `SECTIONS` sabiti; hiçbir durum okumaz,
hiçbir aksiyon yollamaz. Ayarlar'ın altında açılır — **yeni rota yok**, dört üst görünüm
korunur (D45/D40). Altı bölüm: antrenman, ilerleme, program, yedek (en önemli), kurulum/
güncelleme, sınırlar. Sınırlar bölümü bilinçli olarak dürüst: uygulama kapalıyken hiçbir şey
çalışmaz, kalıcı depolama garanti değildir, "tüm verileri sil" geri alınamaz.

### 5. KL-C'nin tamamı için tek konsolide liste

§6'da. 1.0 mührü onunla atılacak.

## 3. Mimarın bakması gereken karar noktaları

- **`AppState.profile?` (F4.5) — şema kararı.** Kişisel bilgiler için **salt-eklemeli
  isteğe bağlı** bir alan eklendi; **şema `v:2` kaldı** (S5'te `Run.endedAt` ile kurulan
  içtihat: salt-eklemeli alan sürüm artırmaz, kırıcı değişiklik `vN→vN+1`). Alanlar:
  `name?`, `birthYear?`, `heightCm?`, `updatedAt`. `updatedAt` **birleştirme için** var:
  profil tek nesne olduğundan `byStable` (kararlı-seri) seçimi kullanıcının az önce
  yazdığı adı kaybettirebilirdi; kayıtlarla aynı **LWW** kuralı uygulandı. Profilsiz eski
  veri geçerli kalır (testli). Profil **yedeğe girer** (sayaçtan farklı olarak soyulmaz);
  hiçbir yere gönderilmez, UI metni bunu açıkça söylüyor. Kabul mü?
- ~~**`BackupServices` iki alan büyüdü**~~ — **mimar onayıyla `AppServices` oldu** (§8).
  Bu bir **port değil** (`app/ports.ts` S6'da hiç değişmedi); main.ts'in kabloladığı
  uygulama yetenekleri arayüzü.
- **`exportBackup` → `exportFile(content, filename, mime)`** genelleştirildi; `exportBackup`
  ve `exportCsv` ince sarmalayıcı. **Şart 1 (jest-senkron) korundu:** onClick'ten `share()`e
  giden yolda hâlâ `await` yok; ortak `ExportButton` bileşeni `run()`'ı doğrudan onClick
  gövdesinde çağırır. *Yapı kısıtı; Playwright yakalayamaz (S3 içtihadı).*
- **Çubuk yığını (`.barstack`)** — S3 notundaki cila maddesi: sayaç ve güncelleme çubuğu
  artık üst üste binmiyor, alt alta diziliyor (güncelleme üstte, sayaç alt bara yaslı).
  Ölçüldü (§4). D41'in davranışı değişmedi.
- ~~**S5'in bıraktığı iki cila maddesi yapılmadı**~~ — **mimar eki ile kapandı:** bunlar
  kapsam dışı değil, ROADMAP **F3.2'nin eksik kalanı**ydı ("peşin kararlar kapsamı
  netleştirir, daraltmaz" — D50 ek). Yapıldı; ayrıntı §8.
- **Hatırlatmanın "hiç yedek yok" hâli ilk kayıtta çıkar.** `reason:'never'` kayıt varken
  hemen uyarır (yaş ölçülemez). Semantik doğru ama ilk antrenmanın ortasında görünür;
  "Şimdi değil" oturumluk susturur. İstenirse tek satırlık politika değişikliği: ilk kayıt
  üstünden N gün geçmeden 'never' uyarmasın. **Mimar kararı bekliyor** — şu an değişmedi.
- **`docs/proposals/` boş kaldı:** S6'da kilitli hiçbir karardan sapılmadı, karar önerisi yok.

## 4. Doğrulamalar (gerçek Chromium, üretim derlemesi + preview)

Geçici Playwright koşuları; **commit edilmedi** (duman testi S0'dan beri iki test).

- **CSV app-path (indirme ayağı):** Antrenman'da `62,5 × 8` girildi → Ayarlar → "CSV dışa
  aktar" → indirilen dosya `trainlog-2026-08-27.csv`, içerik:
  `﻿` + başlık + `1;2026-08-27;Pull;1;Barbell Row;3;6-8;2-3;62,5;8;;;;;;;;;500;;` — BOM,
  ayraç, virgüllü ondalık, sürüm hedefleri ve hacim (500) yerinde.
- **Hatırlatma:** `lastBackup` 30 gün geriye alındı → reload → Antrenman'da "Yedek
  hatırlatması · 30 gündür yedek yok (eşik 21 gün)"; Ayarlar "Son yedek: 30 gün önce (…)".
  CSV aktarımından **sonra da** "30 gün önce" — CSV yaşı sıfırlamadı.
- **Profil:** "Ad: Deneme" → `state.profile = {name:'Deneme', updatedAt:…}`.
- **Kılavuz:** açılıyor; "Geri yükleme EZMEZ, BİRLEŞTİRİR" görünür.
- **Silme:** ilk onayda kayıt sayısı 1 (dokunulmadı) → "Evet, hepsini sil" → kayıt 0, seans 0,
  profil yok, `lastBackup 0`, katalog **22 hareket**, aktif koşu **1**; `caches.keys()`
  silme öncesi/sonrası aynı (D36).
- **Erişilebilirlik taraması (390×844):** dört sekme + İlerleme'nin üç segmenti + Program
  düzenle/katalog + Ayarlar (silme onayı dahil) + kılavuz tarandı; **44px altında dokunma
  hedefi yok**. Tarama iki gerçek kusur buldu, ikisi de düzeltildi (Wake Lock satırı,
  Ölçüm tarihi etiketi) — ayrı commit.
- **Çubuk yığını:** sayaç açıkken güncelleme çubuğu yığına enjekte edildi →
  `updateBottom 727 = timerTop 727`, `timerBottom 792 = tabbarTop 792` → **binme yok**.
- **Kontrast (hesaplandı, WCAG AA ≥ 4.5):** muted/surface **6.85**, muted/bg 7.5,
  metin/surface 14.2, accent/surface **5.37**, accent/bg 5.88, ok 8.98, sarı uyarı 8.79,
  kırmızı 6.33, düğme (bg üstü accent) 5.88, güncelleme çubuğu 5.78. En düşük 5.37.
- **`prefers-reduced-motion`:** tek geçiş (`.tab` renk) **yalnızca** `no-preference`
  bloğunda tanımlı; `reduce` tercihinde hiçbir geçiş tanımlı değil. Kural CSS'te yorumla sabitlendi.
- **Konsol:** uygulama hatası yok (koşu boyunca `console.error` = 0).

## 5. Bütçeler (D35)

- **JS gzip ~24,8 KB** + CSS gzip ~1,8 KB → toplam ~26,6 KB ≪ **~150 KB** tavan.
  (S5: ~20 KB; artış kılavuz metni + CSV + yeni bölümler + F3.2 düzenleme formu.)
- **Durum nesnesi:** taze (tohumlu) durum **5,2 KB**; sentetik 1000 kayıt eklenince
  **159 KB** → kayıt başına ~154 bayt. 1 MB tavanı ~6.500 kayda karşılık gelir (yıllarca
  antrenman). Rahat.
- Konsolda hata yok.

## 6. KL-C — SAHİBE KONSOLİDE CİHAZ LİSTESİ (1.0 mührü bunun üstüne atılır)

Hepsi **kurulu uygulamada** (Ana Ekrana Ekle), gerçek iPhone'da. Safari sekmesi ayrı
depolama bölmesidir — test ederken karıştırma.

**A. Kurulum ve kabuk (S0)**
1. [ ] Safari → Paylaş → Ana Ekrana Ekle; ikon net, tam ekran, safe-area doğru (çentik/alt bar).
2. [ ] Bir kez çevrimiçi aç (önbellek dolsun) → **uçak modunda** aç: uygulama açılıyor.
3. [ ] Ayarlar'da "Kalıcı depolama: açık" görünüyor (kurulu modda).

**B. Kayıt döngüsü (S2-S3)**
4. [ ] Önerilen gün doğru; set/tekrar girişi (virgüllü ondalık `62,5`), "+ Set", "geçeni al",
   RIR/not. Klavye açıkken alt bar davranışı kabul edilebilir.
5. [ ] Uygulamayı kapat/aç → girilen veri **ve gösterim** duruyor.
6. [ ] İkame: hareketi o seanslığına değiştir → program **etkilenmiyor** (Program sekmesinde kontrol).
7. [ ] "Bitir" → seans özeti; sonra takvimde aynı özet açılıyor.

**C. Sayaç (S3)**
8. [ ] Dinlen → telefonu kilitle → aç: **kalan süre doğru**; bitişte görsel uyarı (ses yok).
9. [ ] "Antrenmanda ekranı açık tut" anahtarı ekran kilidini erteliyor.

**D. Yedek zinciri (S3 + S6) — en kritik blok**
10. [ ] Ayarlar → **Dışa aktar** → paylaşım sayfası → **Dosyalara Kaydet** çalışıyor.
11. [ ] **Yedekten yükle** (dosyadan): geri yükleme **birleştiriyor**, ezmiyor; sonuç satırı
    ("N yeni kayıt, M güncelleme") mantıklı.
12. [ ] Safari sekmesi ↔ kurulu uygulama ayrımı ilk-açılış "Yedekten yükle" yoluyla aşılıyor.
13. [ ] **Yeni:** "Son yedek: … gün önce" doğru görünüyor; 21 günü geçince Antrenman ekranında
    hatırlatma çıkıyor, yedek alınca kayboluyor. ("Şimdi değil" yalnız o oturumluk susturur.)
14. [ ] **Yeni:** **CSV dışa aktar** → paylaşım sayfası → Dosyalara Kaydet; dosyayı Numbers/
    Excel'de aç: **sütunlar ayrık**, Türkçe karakterler doğru, ondalık virgüllü.
    CSV aldıktan sonra "Son yedek" **değişmemeli**.

**E. Program (S5 + S6 eki)**
15. [ ] Program salt-okunur açılıyor; Düzenle → Kaydet **yeni sürüm** yaratıyor; geçmiş
    seansların hedefleri **değişmiyor**.
16. [ ] Katalog: yeni hareket, arşivle (silme yok).
16b. [ ] **Yeni (F3.2):** Düzenle ekranında bir yuvanın **RIR alt/üst** değerlerini değiştir,
    bir yuvayı **çıkar**, bir yuvayı **↑/↓ ile sırala**, katalogdan **yeni hareket ekle**
    (arşivli hareket listede çıkmamalı) → Kaydet. Sonra: (a) Antrenman ekranında yeni sıra
    ve yeni hedefler görünüyor, (b) takvimden **eski bir seansı** aç — hedefleri ve hareket
    sırası **eskisi gibi**. Yarım RIR (yalnız bir alan dolu) girildiğinde Kaydet kilitlenmeli.

**F. Yayın cilası (S6)**
17. [ ] **Kullanım kılavuzu** okunabilir, metinler doğru/anlaşılır (dil kontrolü sende).
18. [ ] **Kişisel bilgiler**: ad/doğum yılı/boy giriliyor, kapat-aç sonrası duruyor.
19. [ ] **Tüm verileri sil**: çift onay hissi doğru mu (yanlışlıkla basılmıyor mu);
    sildikten sonra uygulama ilk açılış gibi (tohum program, 22 hareket) ve **çevrimdışı
    açılmaya devam ediyor** (SW önbelleği silinmedi).
    ⚠️ **Bu adımı en sonda ve yedeğini aldıktan sonra dene.**
20. [ ] Dokunma hedefleri parmakla rahat; koyu tema/kontrast gözü yormuyor; sistemde
    "Hareketi Azalt" açıkken tuhaflık yok.

**G. Güncelleme (S0/D41)**
21. [ ] Yeni dağıtımdan sonra alt barda "Güncelleme hazır — Yenile" çıkıyor; dokununca
    yenileniyor, **veri kaybı yok**; kendiliğinden yenilenmiyor.
22. [ ] Sayaç ve güncelleme çubuğu aynı anda çıkarsa üst üste binmiyor (masaüstünde ölçüldü,
    cihazda göz kontrolü).

## 7. Sprint sonrası

S6 sonrası **sprint yok**. Bilinçli-ertelenenler (senkron D11, IndexedDB D23, hareket
görselleri D30, kullanıcı medyası D31, pasif ikame önerisi D47) **tetiksiz açılmaz**.
Bu sprintte hiçbir tetik gerçekleşmedi.

## 8. Ek tur (aynı gün, mimar eki) — dört iş

Sprint kapandıktan sonra mimarın bağladığı işler. Hepsi ayrı, tek konulu commit.

### 8.1 DECISIONS — D20 ek + D50 ek (verbatim)

Sahibin yazdığı, mimar onaylı metin **birebir** işlendi (D50 ek bunu açıkça izinli kılıyor).
Metne dokunulmadı; tek ek düzenleme başlıktaki "Son güncelleme" tarihi (2026-08-14 → 2026-08-27).
Böylece S5-S6'da sprint notlarında taşınan iki içtihat (salt-eklemeli alan → sürüm artmaz;
süreç/raporlama kuralları) artık **karar kaydında** duruyor, notlarda değil.

### 8.2 `BackupServices` → `AppServices`

Mekanik yeniden adlandırma, davranış değişikliği yok; 6 dosya, 14 satır. `app/ports.ts`'e
dokunulmadı. Sprint boyunca "adı dar kaldı" diye işaretlenen nokta kapandı.

### 8.3 F3.2'nin eksik kalanı — program düzenlemede RIR + yuva işlemleri

**İçtihat uygulandı:** bunlar "kapsam dışı" değil, F3.2'nin eksik kalanıydı; mimarın peşin
kararları kapsamı **netleştirir, daraltmaz** (D50 ek). Yapılanlar — hepsi **mevcut**
düzenle → yeni-sürüm yolunda (`nextProgramVersion` → `saveProgramVersion`), **yeni aksiyon yok**:

- **RIR alanı** — her yuvada RIR alt/üst. Form iki ayrı alan tuttuğu için "yarım giriş"
  temsil edilebilmeli; domain tipi (`rir?: [number, number]`) edemez, **doğrulama eder**:
  `validateRirInput(lo, hi)` → `rir-incomplete` | `rir-lo-gt-hi` | `rir-negative`
  (kod döner, Türkçe UI'da — S1 içtihadı, S5'teki desenin aynısı). `validatePrescribed`
  artık `rir`i de denetliyor. Geçersizken **Kaydet kilitli**.
- **Yuvaya katalogdan hareket ekleme** — `selectors.activeExercises` ile **arşivli hariç**
  (arşivli hareket yeni sürüme eklenmez, S5 kuralı). Varsayılan yeni yuva: 3×8-12, 90 sn, RIR yok.
- **Yuva çıkarma** ve **↑/↓ sıralama** — `removeDayItem` / `moveDayItem`.
- Üç yardımcı da `domain/program.ts`'te **saf, değişmez, aralık dışı no-op**. `ItemsDay<T>`
  genel tutuldu: düzenleme formunun taslak öğeleri (ayrık RIR alanları) **aynı** mantığı
  kullanıyor — UI'da kopya sıralama/ekleme kodu yok.
- `aria-label`'lar güne göre benzersizleştirildi (`Gün 1 2. yuvayı çıkar`) — aynı etiketin
  beş günde tekrarlanması hem erişilebilirlik hem test açısından belirsizdi.

**Kapı testi genişletildi** (S5 kapı testinin üstüne): dört düzenleme tipi bir arada
(RIR değişimi + yuva çıkarma + sıralama + ekleme) → tek `saveProgramVersion`. Kanıtlananlar:
geçmiş seans **v1**'de kalıyor (yuva **sırası**, **RIR**'i ve **çıkarılan yuvası** yerinde),
`sessionSummary` aynı; yeni seans **v2**'yi görüyor (yeni sıra, çıkarılan hareket yok).
Ayrıca `validateProgram(v2)` boş — düzenleme geçerli program üretiyor. 9 yeni test (toplam 138).

**Önizleme kanıtı (gerçek Chromium, üretim derlemesi):**
`sürüm 1` → Düzenle → RIR üst boşaltıldı: **"RIR: iki alan da dolmalı"**, Kaydet **kilitli**
→ RIR 2/1: **"RIR alt > üst"**, hâlâ kilitli → RIR 1-2: Kaydet açık → 2. yuva çıkarıldı,
2. yuva yukarı taşındı, katalogdan **Face Pull** eklendi → Kaydet → **`sürüm 2`**.
Depoda: `prog_main_v1` **bayt olarak aynı** (`rir [2,3]`, altı yuva, sıra korunmuş),
seans hâlâ `prog_main_v1`'e bağlı; `fam_main_r2` yeni sırayı + `rir [1,2]` + eklenen yuvayı
taşıyor; koşunun işaretçisi `fam_main_r2`. Konsol hatası yok.
**44px yeniden taraması:** düzenleme ekranında 44px altında dokunma hedefi **yok**.

### 8.4 Kök `README.md` (depo vitrini)

Türkçe; ne yapar / neden böyle / yığın / mimari özeti / komutlar / belge bağlantıları +
üç ekran görüntüsü (`docs/shot-*.png`, gerçek Chromium 390×844, **sentetik** veri — D49).
Canlı bağlantı `https://hasandemirr.github.io/TrainLog/` bugün **200** dönüyor; ancak
`origin/main` **S5'te** (`7c403f7`) olduğu için **yayındaki sürüm S5'tir** — S6 ve bu ek
turu **yerelde, push sende** (bu satır origin'den doğrulandı, hafızadan değil).

### 8.5 Ek tur sonrası kapı

`typecheck` temiz · `vitest` **138** yeşil · `build` yeşil (**JS 24,8 KB** + CSS 1,8 KB gzip)
· Playwright dumanı **2/2** · geçici doğrulama koşuları silindi, çalışma ağacı temiz.
KL-C'ye **16b** maddesi eklendi (§6/E) — F3.2 düzenlemesinin cihazda denenmesi.
